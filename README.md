# Dina Holdings LLC — Next.js App (App Router, TypeScript)

## Run locally
```bash
npm install
npm run dev
```

## Build & start
```bash
npm run build
npm start
```

This project uses a single-page layout in `app/page.tsx` with inline-injected CSS for performance and simplicity.
Images are served from Unsplash (whitelisted in `next.config.mjs`). Metadata, robots.txt and sitemap.xml included.
